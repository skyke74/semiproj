CREATE TABLE premium_room_reservation (
    reservation_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    hotel_id INT NOT NULL,
    guest_name VARCHAR(100) NOT NULL,
    check_in DATE NOT NULL,
    check_out DATE NOT NULL,
    room_info VARCHAR(100) NOT NULL,
    price INT NOT NULL, -- 객실 가격 (premium_price에서 가져옴)
    FOREIGN KEY (user_id) REFERENCES Users(user_id),
    FOREIGN KEY (hotel_id) REFERENCES Hotels(hotel_id)
);

INSERT INTO premium_room_reservation (user_id, hotel_id, guest_name, check_in, check_out, room_info, price)
VALUES (
    ?,                                        -- Users 테이블의 user_id
    ?,                                       -- Hotels 테이블의 hotel_id
    '',                               -- 손님 이름
    '',                             -- 체크인 날짜
    '',                             -- 체크아웃 날짜
    '',                            -- 객실 정보
    (SELECT premium_price FROM Hotels WHERE hotel_id = ?)  -- Hotels에서 프리미엄 가격 가져오기
);